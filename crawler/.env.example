LINKEDIN_EMAIL=your_email
LINKEDIN_PASSWORD=your_email_password

# RabbitMQ Configuration
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USER=guest
RABBITMQ_PASS=guest
RABBITMQ_QUEUE=linkedin_profiles

# Crawler Speed Configuration (in seconds)
# Lower values = faster crawling, but may trigger LinkedIn rate limits
# Recommended: MIN_DELAY=0.5, MAX_DELAY=1.0 for balanced speed
# Aggressive: MIN_DELAY=0.3, MAX_DELAY=0.6 (faster but riskier)
# Safe: MIN_DELAY=1.5, MAX_DELAY=3.0 (slower but safer)
# Balanced (recommended for complete data): MIN_DELAY=0.8, MAX_DELAY=1.5
MIN_DELAY=0.8
MAX_DELAY=1.5
